---
extends: default

rules:
    line-length: disable
    indentation:
        spaces: consistent
        indent-sequences: consistent
        check-multi-line-strings: true
    empty-lines:
        max: 1
